Inbox check for [YOUR_NAME]. Review the inbox via your email MCP for anything
requiring action or attention.

Look for:
- Messages needing a reply (prioritise anything time-sensitive or from important contacts)
- Anything that should become a task — if so, create it via your task MCP with appropriate
  due date and priority
- Anything worth remembering long-term — store it in memory with the appropriate user_id

Query memory for [YOUR_NAME]'s important contacts and ongoing projects so you can
correctly identify what is actually important vs. routine.

If you find actionable items, report them briefly:
- Sender name and subject
- What action is needed and by when (if clear)
- Whether you've already created a task for it

Keep it to two or three lines per item. Do not quote email content verbatim.

If the inbox contains nothing requiring attention, respond with exactly this
and nothing else:
NOTHING_TO_REPORT
