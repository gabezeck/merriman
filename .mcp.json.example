{
  "mcpServers": {
    "todoist": {
      "_comment": "Todoist task manager. Uses Todoist's official MCP endpoint.",
      "_docs": "https://todoist.com/help/articles/use-mcp-with-todoist",
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://ai.todoist.net/mcp"]
    },

    "google-calendar": {
      "_comment": "Google Calendar. Requires OAuth credentials (see docs/getting-started.md).",
      "_docs": "https://github.com/cocal/google-calendar-mcp",
      "command": "npx",
      "args": ["-y", "@cocal/google-calendar-mcp"],
      "env": {
        "GOOGLE_OAUTH_CREDENTIALS": "/path/to/gcp-oauth.keys.json"
      }
    },

    "gmail": {
      "_comment": "Gmail. Various MCP server implementations available — pick one.",
      "_docs": "https://github.com/google-research/google-research/tree/master/gmail_mcp",
      "command": "npx",
      "args": ["-y", "@gongrzhe/server-gmail-autoauth-mcp"]
    },

    "openmemory": {
      "_comment": "OpenMemory — long-term semantic memory (CaviraOSS/OpenMemory, self-hosted).",
      "_setup": "Clone and start via docker-compose.yml. See docs/getting-started.md.",
      "_key": "x-api-key must match OM_API_KEY in your .env file.",
      "type": "http",
      "url": "http://localhost:2444/mcp",
      "headers": {
        "content-type": "application/json",
        "accept": "application/json, text/event-stream",
        "x-api-key": "YOUR_OM_API_KEY"
      }
    },

    "guardian": {
      "_comment": "The Guardian — news search and article retrieval. Free API key.",
      "_docs": "https://open-platform.theguardian.com/access/",
      "command": "npx",
      "args": ["guardian-mcp-server"],
      "env": {
        "GUARDIAN_API_KEY": "YOUR_GUARDIAN_API_KEY"
      }
    },

    "open-meteo": {
      "_comment": "Weather forecasts. No API key required.",
      "_docs": "https://open-meteo.com",
      "command": "npx",
      "args": ["-y", "-p", "open-meteo-mcp-server", "open-meteo-mcp-server"]
    }
  }
}
