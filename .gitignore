node_modules/

# Env files
.env*
!.env.example
config.yaml

# MCP config (contains API keys — use .mcp.json.example as the template)
.mcp.json

# Agent user data (logs, inboxes, personal content)
# The agents/ directory structure and example agents are tracked.
logs/
agents/*/inbox/

# Python
.venv/
__pycache__/
*.pyc

# Third-party source clones (cloned by setup, not committed)
services/

# Docker override (user-specific customisations)
docker-compose.override.yml
